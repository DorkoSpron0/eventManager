server:
  port: 8787
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/eventManagerIas"
    username: "root"
    password: "root"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  packages-to-scan: com.ias.eventManagerRun.infrastructure.entry_points
  paths-to-match: "/iasapi/**"  # Ajusta según tu estructura de rutas