server:
  port: 8787
spring:
  datasource:
    url: "jdbc:mysql://db:3307/eventManagerIas"
    username: "root"
    password: "root"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
springdoc:
  packages-to-scan: com.ias.eventManagerRun.infrastructure.entry_points
  paths-to-match: "/iasapi/**"  # Ajusta según tu estructura de rutas